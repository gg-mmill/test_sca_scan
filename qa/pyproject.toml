[project]
name = "qa"
version = "0.1.0"
description = "Default template for PDM package"

dependencies = [
    "cg==60.0.0",
]
requires-python = "==3.11.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
